# Progress Log — 2026-05-27T18:20:38+0000

Progress audit of the repo against `docs/technical_steps.md`. Status reflects
files actually present under `src/` and `src/species/processed_data/`.

## Summary

| Area | Status |
|---|---|
| **Task 2 (species / Evo 2)** | End-to-end pipeline written **and run** through probes + UMAP. This is the working vertical slice. |
| **Task 1 (regulatory)** | Scripts written (`01_build_windows.py`, `extract_embeddings.py`) but **not run** — no `processed_data/` under `src/regulatory/`. |
| **Multi-model comparison** | Only **Evo 2** has been run. HyenaDNA / Caduceus / NT v2 / DNABERT-S not yet executed. |
| **Baselines** | k-mer TF–IDF and 1D CNN baselines **not implemented**. |

---

## Step-by-step against `technical_steps.md`

### 1. Environment & infra — ✅ mostly done
- `pyproject.toml` pins torch 2.7.1 (cu128 index), `evo2`, `transformers`, `peft`,
  `accelerate`, `scikit-learn`, `pyfaidx`/`pysam`, `pybedtools`/`pyranges`,
  Biopython, `dendropy`, `ete3`, `h5py`.
- ⚠️ `umap-learn` listed in the technical steps is **not** in `pyproject.toml`,
  yet `06_umap_visualization.py` exists and produced output — confirm it's
  actually installed / pinned.
- Caching layout implemented as one HDF5 per layer (`blocks_<n>_mlp_l3.h5`), matching the "HDF5 per model" option.

### 2. Task 1 — regulatory data pipeline — 🟡 written, not run
- `src/regulatory/01_build_windows.py` implements gene-body vs regulatory
  interval construction, GC matching, and the chromosome-level split
  (train chr1–18 / val chr19–20 / test chr21–22,X,Y). Matches steps 2.1–2.5.
- **No outputs**: `src/regulatory/README.md` is empty and there is no
  `processed_data/` directory for regulatory. The script has not been executed
  (data files: GENCODE GTF, ENCODE cCRE BED, hg38 FASTA must be downloaded first).

### 3. Task 2 — species data pipeline — ✅ done
- `00_download_data.sh` — GTDB r232 taxonomy/metadata/trees + NCBI datasets CLI. Written.
- `01_subsample_species.py` — ran: `species_index.tsv` has **200 species** (stratified across phyla, Bacteria + Archaea).
- `02_extract_fragments.py` — ran: `fragments.tsv` has **10,000 fragments** (1024 bp), species-level split 140 train / 20 val / 40 test species (7000/1000/2000 fragments). Matches step 3 design (held-out species, N-filtering, non-overlapping windows).
- `03_build_phylo_distance.py` — ran: `phylo/distance_matrix.npz` + `accessions_in_tree.txt` cached.

### 4. Model loading & frozen embedding extraction — 🟡 partial (Evo 2 only)
- `04_extract_embeddings_evo2.py` (species) and `extract_embeddings.py` (Task 1,
  5 models) both written. Mean-pool over sequence axis per layer → HDF5. ✅ design matches.
- **Run status**: only Evo 2 species embeddings exist. `frag_ids.txt` has **9,999** rows.
- ⚠️ **Discrepancy to resolve**: only `embeddings/evo2/blocks_28_mlp_l3.h5` is
  on disk, but the results files (`classification_results.tsv`, UMAP PNGs) are
  for layers **blocks.16** and **blocks.26**. The h5 files for 16/26 appear to
  have been deleted/regenerated after the results were produced. Re-extract or
  restore those layers before re-running probes. (Last commit: "ran script 04 for 1 layer for species".)
- ⚠️ 9,999 frag_ids vs 10,000 fragments — one fragment dropped during extraction; worth confirming it's intentional (e.g., an N-filter or OOM skip).

### 5. Probes — ✅ run (Evo 2, 2 layers)
- `05_train_probes.py` ran: L2 logistic + shallow MLP on phylum & species.
- Results (`results/classification_results.tsv`):
  - **Phylum** (real generalization metric): logistic AUC **0.96**, AUPRC **0.77**, F1 **0.69–0.70** at both layers 16 and 26. Strong.
  - **Species** F1 = 0.0 — expected by construction (disjoint train/test species; kept as no-information baseline).
- Standardization / regularization tuning per the step: implemented in script (verify val-tuning is wired).

### 6. Baselines — ❌ not started
- No k-mer TF–IDF + logistic baseline.
- No shallow 1D CNN on one-hot sequence.
- These are needed to contextualize the 0.96 phylum AUC.

### 7. Evaluation — 🟡 partial
- Task 2 classification metrics: ✅ AUC/AUPRC/F1 computed and layer curves plotted (`layer_curves_phylum.png`, `layer_curves_species.png`).
- Task 2 embedding distance: ✅ computed (`phylo_correlation.tsv`, `phylo_curves.png`):
  - Spearman ρ vs GTDB tree distance is **weak**: cosine ρ = 0.037 (layer 16) → 0.159 (layer 26); euclidean ρ = 0.018 → 0.187. n_pairs = 1,359,000.
  - **Signal increases with depth** (layer 26 > layer 16) but overall correlation is low — flag for discussion / more layers.
- Task 1 evaluation: ❌ blocked on Task 1 pipeline not being run.
- Bootstrap CIs over windows/species: ❌ not yet implemented.

### 8. Analysis & writeup — 🟡 partial
- ✅ Layer-wise probe curves (phylum/species) and phylo curves for Evo 2.
- ✅ UMAP colored by phylum and by domain for layers 16 & 26 (`results/umap/`).
- ❌ cCRE-class UMAP (Task 1) — blocked on Task 1.
- ❌ Long-context vs short-context model comparison — only Evo 2 has been run.

---

## Highest-leverage next steps
1. **Re-extract Evo 2 embeddings for layers 16 & 26** (or run more layers) so the
   on-disk h5 set matches the reported results, then sweep more of the 32 blocks
   for proper layer-wise curves.
2. **Run the Task 1 regulatory pipeline**: download GENCODE/ENCODE/hg38, run
   `01_build_windows.py`, then `extract_embeddings.py`, then build Task 1 probes.
3. **Run the other DNA LMs** (HyenaDNA, Caduceus, NT v2, DNABERT-S) for the
   cross-architecture comparison that is the core of the proposal.
4. **Implement baselines** (k-mer TF–IDF + logistic, 1D CNN).
5. Add **bootstrap CIs**; investigate the weak phylo-distance correlation.

## Open questions / risks
- `umap-learn` not pinned in `pyproject.toml` despite being used.
- 9,999 vs 10,000 fragment count mismatch.
- Missing h5 layers vs reported result layers (reproducibility gap).
- No Task 1 outputs at all yet — half the proposal is unstarted.
